namespace CombatEngine.Enums;

// Purely an authoring-format concern, mirroring BuffDebuffType; converted to a bool isPositive at
// the CombatFunction boundary.
public enum RegenDrainType
{
    Positive,
    Negative
}
