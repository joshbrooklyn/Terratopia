{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Job",
  "description": "Terratopia job definition (src/GameEngine/DataClasses/Job.cs)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "jobId",
    "name",
    "hpBase",
    "tpBase",
    "powerBase",
    "defenseBase",
    "speedBase",
    "hpPerLevel",
    "tpPerLevel",
    "powerPerLevel",
    "defensePerLevel",
    "speedPerLevel"
  ],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 2,
      "description": "Schema version this file was written against. Managed by the GameData Editor — do not hand-edit."
    },
    "jobId": {
      "type": "string",
      "description": "Unique identifier for this job."
    },
    "name": {
      "type": "string",
      "description": "Display name."
    },
    "hpBase": {
      "type": "integer",
      "description": "The level-0 floor of the formula: stat = base + level * growth."
    },
    "tpBase": {
      "type": "integer",
      "description": "The level-0 floor of the formula: stat = base + level * growth."
    },
    "powerBase": {
      "type": "integer",
      "description": "The level-0 floor of the formula: stat = base + level * growth."
    },
    "defenseBase": {
      "type": "integer",
      "description": "The level-0 floor of the formula: stat = base + level * growth."
    },
    "speedBase": {
      "type": "integer",
      "description": "The level-0 floor of the formula: stat = base + level * growth."
    },
    "hpPerLevel": {
      "type": "integer",
      "description": "Hp gained per level, applied including level 1 (stat = base + level * growth)."
    },
    "tpPerLevel": {
      "type": "integer",
      "description": "Tp gained per level, applied including level 1 (stat = base + level * growth)."
    },
    "powerPerLevel": {
      "type": "integer",
      "description": "Power gained per level, applied including level 1 (stat = base + level * growth)."
    },
    "defensePerLevel": {
      "type": "integer",
      "description": "Defense gained per level, applied including level 1 (stat = base + level * growth)."
    },
    "speedPerLevel": {
      "type": "integer",
      "description": "Speed gained per level, applied including level 1 (stat = base + level * growth)."
    }
  }
}
